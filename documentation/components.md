# Components Library
Core reusable UI:
- `.ab-card`, `.ab-stat`, `.ab-timeline`
- `.ab-sidebar`, `.ab-header`, `.ab-customizer`
- Bootstrap 5 elements with style-safe overrides

Each component page in `/html/` demonstrates practical enterprise usage patterns.
